name: OpenClaw persistent signed device v7

on:
  push:
    branches:
      - openclaw/browser-probe-20260828
    paths:
      - .github/workflows/openclaw-persistent-device-v7.yml

permissions:
  contents: read
  issues: write

jobs:
  connect:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4

      - name: Publish ephemeral transport key
        id: key
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          run_key="${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          umask 077
          openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -out /tmp/transport-private.pem >/dev/null 2>&1
          openssl pkey -in /tmp/transport-private.pem -pubout -out /tmp/transport-public.pem >/dev/null 2>&1
          public_b64="$(base64 -w0 /tmp/transport-public.pem)"
          body="$(printf 'IDOL_OPENCLAW_KEY:%s\n%s' "$run_key" "$public_b64")"
          jq -n --arg body "$body" '{body:$body}' >/tmp/key-comment.json
          curl -fsS -X POST \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/issues/1/comments" \
            --data-binary @/tmp/key-comment.json >/dev/null
          echo "run_key=$run_key" >>"$GITHUB_OUTPUT"

      - name: Receive encrypted device bundle
        id: receive
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_KEY: ${{ steps.key.outputs.run_key }}
        shell: bash
        run: |
          set -euo pipefail
          cipher=""
          for _ in $(seq 1 240); do
            curl -fsS \
              -H "Authorization: Bearer ${GH_TOKEN}" \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "https://api.github.com/repos/${GITHUB_REPOSITORY}/issues/1/comments?per_page=100" \
              >/tmp/comments.json
            cipher="$(python3 - "$RUN_KEY" /tmp/comments.json <<'PY'
          import json, sys
          key, path = sys.argv[1], sys.argv[2]
          marker = f"IDOL_OPENCLAW_CIPHERTEXT:{key}"
          for comment in reversed(json.load(open(path))):
              body = comment.get("body") or ""
              if body.startswith(marker):
                  rest = body[len(marker):]
                  if rest.startswith("\\n"): rest = rest[2:]
                  elif rest.startswith("\n"): rest = rest[1:]
                  print(rest.strip())
                  break
          PY
            )"
            if [ -n "$cipher" ]; then break; fi
            sleep 2
          done
          if [ -z "$cipher" ]; then
            printf '%s\n' '{"schema":"idol.openclaw.persistent-connect.v1","outcome":"ciphertext-timeout"}' >connect-result.json
            echo "received=false" >>"$GITHUB_OUTPUT"
            exit 0
          fi
          printf '%s' "$cipher" | base64 -d >/tmp/device-cipher.bin
          openssl pkeyutl -decrypt \
            -inkey /tmp/transport-private.pem \
            -in /tmp/device-cipher.bin \
            -pkeyopt rsa_padding_mode:oaep \
            -pkeyopt rsa_oaep_md:sha256 \
            -out /tmp/idol-openclaw-input.json
          jq -r '.sharedToken, .deviceToken // empty, .device.privateKeyPem' /tmp/idol-openclaw-input.json | while IFS= read -r secret; do
            [ -n "$secret" ] && echo "::add-mask::$secret"
          done
          echo "received=true" >>"$GITHUB_OUTPUT"

      - name: Install WebSocket transport and local envelope key
        if: steps.receive.outputs.received == 'true'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/idol-openclaw-node
          cd /tmp/idol-openclaw-node
          npm init -y >/dev/null 2>&1
          npm install --silent ws@8
          cp "$GITHUB_WORKSPACE/scripts/openclaw-persistent-device-connect-v7.mjs" ./connect.mjs
          cat >/tmp/management-public.b64 <<'EOF'
          
          EOF
          tr -d '[:space:]' </tmp/management-public.b64 | base64 -d >/tmp/idol-management-public.pem

      - name: Connect persistent signed device
        if: steps.receive.outputs.received == 'true'
        shell: bash
        run: |
          set -euo pipefail
          cd /tmp/idol-openclaw-node
          OPENCLAW_INPUT_FILE=/tmp/idol-openclaw-input.json \
          OPENCLAW_MANAGEMENT_PUBLIC_KEY_FILE=/tmp/idol-management-public.pem \
          OPENCLAW_RESULT_FILE="$GITHUB_WORKSPACE/connect-result.json" \
          OPENCLAW_ENVELOPE_FILE="$GITHUB_WORKSPACE/credential-envelope.json" \
            node connect.mjs

      - name: Upload sanitized result and encrypted credential state
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: openclaw-persistent-device-${{ steps.key.outputs.run_key }}
          path: |
            connect-result.json
            credential-envelope.json
          retention-days: 1
          if-no-files-found: error

      - name: Destroy plaintext and transient keys
        if: always()
        shell: bash
        run: |
          rm -f /tmp/transport-private.pem /tmp/transport-public.pem /tmp/key-comment.json \
            /tmp/device-cipher.bin /tmp/idol-openclaw-input.json /tmp/comments.json \
            /tmp/idol-management-public.pem /tmp/management-public.b64
